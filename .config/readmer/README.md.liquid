# {{ project.title }}

{% render 'badge/unlicense' %}
{% render 'badge/rust-package', name: 'distrib' %}
{% render 'badge/ts-package', name: 'distrib' %}
{% render 'badge/dart-package', name: 'distrib' %}
{% render 'badge/python-package', name: 'distrib.sh' %}
{% render 'badge/ruby-package', name: 'distrib' %}

**{{ project.summary }}**

{% render 'header/toc' %}

## ✨ Features

- Available both as the command-line tool [`distrib`] and a polyglot library.
- Polyglot software <sup><sub>(soon!)</sub></sup> available for Dart, Python, Ruby, Rust, and TypeScript.
- Cuts red tape: 100% free and unencumbered public domain software.

## ⬇️ Installation

### Installation of the CLI

#### Installation via [Cargo Binstall]

```bash
cargo binstall -y distrib
```

<img width="100%" alt="Installation via cargo-binstall" src="{{ github.repository.link }}/raw/master/rust/etc/asciinema/install.gif"/>

#### Installation via [mise]

```bash
mise use -g github:artob/distrib
```

#### Installation via [Cargo]

```bash
cargo install distrib --locked --features=cli
```

### Installation of the Library

<details>
<summary>Installation for Rust from Crates.io</summary>

#### Installation from [Crates.io]

```bash
cargo add distrib
```
</details>

<details>
<summary>Installation for JavaScript/TypeScript from NPM</summary>

#### Installation from [NPM]

```bash
npm install distrib
bun add distrib
pnpm add distrib
yarn add distrib
```
</details>

<details>
<summary>Installation for Dart from Pub.dev</summary>

#### Installation from [Pub.dev]

```bash
dart pub add distrib
flutter pub add distrib
```
</details>

<details>
<summary>Installation for Python from PyPI</summary>

#### Installation from [PyPI]

```bash
pip install -U distrib.sh
uv add distrib.sh
poetry add distrib.sh
pdm add distrib.sh
```
</details>

<details>
<summary>Installation for Ruby from RubyGems</summary>

#### Installation from [RubyGems]

```bash
gem install distrib
bundle add distrib
```
</details>

## 👉 Examples

## 📚 Reference

### Command-Line Interface

{% render '.config/readmer/distrib.sh-session' %}

#### `distrib inspect`

{% render '.config/readmer/distrib-inspect.sh-session' %}

#### `distrib clean`

{% render '.config/readmer/distrib-clean.sh-session' %}

#### `distrib build`

{% render '.config/readmer/distrib-build.sh-session' %}

#### `distrib publish`

{% render '.config/readmer/distrib-publish.sh-session' %}

## 👨‍💻 Development

```bash
git clone {{ github.repository.url }}
```

---

{% render 'footer/share', title: project.title, url: github.repository.link %}

[`distrib`]: {{ github.repository.link }}#command-line-interface

[Crates.io]: https://crates.io/crates/distrib
[NPM]: https://npmjs.com/package/distrib
[Pub.dev]: https://pub.dev/packages/distrib
[PyPI]: https://pypi.org/project/distrib
[RubyGems]: https://rubygems.org/gems/distrib

[Cargo]: https://rustup.rs
[Cargo Binstall]: https://crates.io/crates/cargo-binstall
[mise]: https://mise.jdx.dev
